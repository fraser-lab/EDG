"""EDG (Ensembles from Density Generator) package main entry point.

This allows running EDG via 'python -m edg' in addition to the 'edg' command.
"""

from edg.cli import main

if __name__ == "__main__":
    main()
